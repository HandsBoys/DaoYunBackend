<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dy.mapper.SysDictTypeMapper">

    <resultMap id="BaseResultMap" type="com.dy.domain.SysDictType">
            <id property="dictId" column="dict_id" jdbcType="BIGINT"/>
            <result property="dictName" column="dict_name" jdbcType="VARCHAR"/>
            <result property="dictType" column="dict_type" jdbcType="VARCHAR"/>
            <result property="createBy" column="create_by" jdbcType="VARCHAR"/>
            <result property="status" column="status" jdbcType="CHAR"/>
            <result property="createTime" column="create_time" jdbcType="TIMESTAMP"/>
            <result property="lastUpdateBy" column="last_update_by" jdbcType="VARCHAR"/>
            <result property="lastUpdateTime" column="last_update_time" jdbcType="TIMESTAMP"/>
            <result property="remark" column="remark" jdbcType="VARCHAR"/>
    </resultMap>

    <sql id="Base_Column_List">
        select
        dict_id,dict_name,dict_type,
        create_by,status,create_time,
        last_update_by,last_update_time,remark
        from sys_dict_type
    </sql>

    <insert id="insertDictType" parameterType="SysDictType">
        insert into sys_dict_type(
        <if test="dictName != null and dictName != ''">dict_name,</if>
        <if test="dictType != null and dictType != ''">dict_type,</if>
        <if test="status != null">status,</if>
        <if test="remark != null and remark != ''">remark,</if>
        <if test="createBy != null and createBy != ''">create_by,</if>
        create_time
        )values(
        <if test="dictName != null and dictName != ''">#{dictName},</if>
        <if test="dictType != null and dictType != ''">#{dictType},</if>
        <if test="status != null">#{status},</if>
        <if test="remark != null and remark != ''">#{remark},</if>
        <if test="createBy != null and createBy != ''">#{createBy},</if>
        sysdate()
        )
    </insert>

    <update id="updateDictType" parameterType="SysDictType">
        update sys_dict_type
        <set>
            <if test="dictName != null and dictName != ''">dict_name = #{dictName},</if>
            <if test="dictType != null and dictType != ''">dict_type = #{dictType},</if>
            <if test="status != null">status = #{status},</if>
            <if test="remark != null">remark = #{remark},</if>
            <if test="lastUpdateBy != null and lastUpdateBy != ''">last_update_by = #{lastUpdateBy},</if>
            last_update_time = sysdate()
        </set>
        where dict_id = #{dictId}
    </update>

    <delete id="deleteDictTypeById" parameterType="Long">
        delete from sys_dict_type where dict_id = #{dictId}
    </delete>

    <delete id="deleteDictTypeByIds" parameterType="Long">
        delete from sys_dict_type where dict_id in
        <foreach collection="array" item="dictId" open="(" separator="," close=")">
            #{dictId}
        </foreach>
    </delete>

    <select id="listDictTypes" resultMap="BaseResultMap">
        <include refid="Base_Column_List"></include>
    </select>

    <select id="checkDictTypeUnique" parameterType="String" resultType="com.dy.domain.SysDictType">
        <include refid="Base_Column_List"/>
        where dict_type = #{dictType} limit 1
    </select>

    <select id="getDictTypeById" parameterType="Long" resultType="com.dy.domain.SysDictType">
        <include refid="Base_Column_List"/>
        where dict_id = #{dictId}
    </select>
</mapper>
